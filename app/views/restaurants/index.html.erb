<% @restaurants.each do |c| %>
  <div class='rest'>
    <%= c.name %>
    <%= link_to "Edit", edit_restaurant_path(c) %>
    <%= link_to 'Delete', c, method: :delete, data: { confirm: 'Are you sure?' } %>

  </div>
<% end %>
