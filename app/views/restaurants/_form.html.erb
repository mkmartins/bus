<%= simple_form_for @restaurant do |f| %>
  <%= f.input :name %>

  <%= f.input :cuisine_id, label: "Cuisine Name", collection: @cuisines %>

  <%= f.simple_fields_for :cuisine do |builder| %>
  <%= builder.input :name, label: "Add Cuisine" %>
  <% end %>

  <%= f.button :submit %>
<% end %>