<%= simple_form_for @restaurant do |f| %>
  <%= f.input :name %>

  <%= f.input :cuisine_id, label: "Cuisine Name", collection: @cuisines %>
  <%= f.input :cuisine, label: "Add Cuisine" %>

  <%= f.button :submit %>
<% end %>