<%= simple_form_for(@cuisine) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, label: "Add Cuisine Name" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
